
.env
.vscode/
node_modules/
docs/
dump/
mongodump.exe
.claude/

# dev/sim script outputs (generated artifacts, kept out of the repo)
scripts/*.csv
scripts/handling-audit-report.md
scripts/provingGrounds.json
economy_sim.csv
scripts/raceweek_tuning.json
preview-serial.png

# creator submissions — operational data mirrored out of Mongo, not game data.
# Mongo is the source of truth; these files are the local recovery copy.
src/submissions/